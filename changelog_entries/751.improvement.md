Removed the `stream` and `version_id` arguments of `inspect_all` and the `driver` and `max_retries` arguments of `inspect_nwbfile`, all of which had been deprecated for removal after 3/1/2025. Use `inspect_dandiset`, `inspect_dandi_file_path`, or `inspect_url` to inspect files on the DANDI archive.
