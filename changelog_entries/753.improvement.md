Simplified `get_package_version` to call `importlib.metadata.version` directly. Its `pkg_resources` fallback was unreachable, since `importlib.metadata` raises `PackageNotFoundError` rather than the `ModuleNotFoundError` it caught, and the project requires Python 3.10.
