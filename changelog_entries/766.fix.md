`check_single_chunk_dataset` no longer flags a small dataset that can still be resized, since HDF5 requires chunked storage for it and such a dataset is usually a seed that a pipeline appends to later.
