Fixed `check_units_table_duration` raising on files whose `spike_times_index` dataset has a signed integer dtype. The leading zero index was `uint64`, and NumPy promotes that mixed with a signed array to `float64`, which cannot be used as a fancy index.
