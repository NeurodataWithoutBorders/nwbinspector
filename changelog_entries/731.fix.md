Fixed the identifier pre-scan in `inspect_all` leaking one open file handle per file and reading every file through PyNWB before the inspection read it again. Only the `/identifier` dataset is now read, with h5py for an HDF5 file and zarr for a Zarr store.
