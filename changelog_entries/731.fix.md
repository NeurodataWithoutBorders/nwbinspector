Fixed the identifier pre-scan in `inspect_all` leaking one open file handle per file and reading every file through PyNWB before the inspection read it again. HDF5 files now have only their `/identifier` dataset read with h5py, and the fallback reader used for Zarr stores closes its IO object.
