Fixed `inspect_all` (and the CLI with `--n-jobs`) failing with a pickling error whenever a config was given together with more than one job. Workers now rebuild the configured check list from the check names and the config instead of receiving function copies that cannot be pickled.
