Fixed the parallel branch of `get_s3_urls_and_dandi_paths` collecting results inside the submit loop, which serialized every request. The helper it calls now also forwards its `follow_redirects` and `strip_query` arguments, and the function accepts an optional `client` like `get_nwb_assets_from_dandiset`.
