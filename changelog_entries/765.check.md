Added `check_single_chunk_dataset` to flag an HDF5 dataset written as a single uncompressed chunk that spans the whole array. Small datasets of that kind are better stored contiguously, and large ones should be split into several chunks and compressed.
