Fixed `check_subject_age` and `check_subject_proper_age_range` raising `ValueError` when the age string contains more than one slash (for example `P1D/P2D/P3D`). The former now returns its usual format message and the latter stays quiet.
